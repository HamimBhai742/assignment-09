import React from 'react';
import { Link } from 'react-router-dom';

const Error = () => {
    return (
        <div>
            <Link to='/'>Go Back</Link>
        </div>
    );
};

export default Error;